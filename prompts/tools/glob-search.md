---
id: glob-search
name: Glob Search
category: retrieval
riskLevel: safe
---

Match files by glob-style patterns inside the current workspace.
Use this to narrow down likely files before reading or editing them.
Prefer targeted patterns such as `*.test.ts`, `src/**/*.tsx`, or `prompts/tools/*.md`.
